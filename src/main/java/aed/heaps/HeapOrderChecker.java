package aed.heaps;

public class HeapOrderChecker {

    /**
     * Does tree satisfy the "heap-order property"?
     * @param tree A binary tree
     * @return True if the tree satisfies the "heap-order property"
     */
    public static <E extends Comparable<E>> boolean satisfiesHeapOrderProperty (BinaryTree<E> tree) {
	return false; 
    }
}
